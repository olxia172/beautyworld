<%= simple_form_for @brand do |f| %>
  <%= f.input :name %>
    <!-- <%= render 'common/error_messages', object: @brand, field_name: :name %> -->
  <%= f.input :logo, type: :file %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
