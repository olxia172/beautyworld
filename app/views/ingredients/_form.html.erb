<%= simple_form_for(@ingredient, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :name %>
  <%= f.association :ingredient_functions, as: :check_boxes %>
  <%= f.button :submit %>
<% end %>
