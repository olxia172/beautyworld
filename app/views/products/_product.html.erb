<div class="col-md-4">
  <%= link_to [@brand, @subcategory, product].compact, class: 'link-to-product' do %>
    <%= image_tag(product.image_url(:thumb)) %>
    <h4><%= product.name %></h4>
    <h6><%= show_categories(product) %></h6>
  <% end %>
</div>
